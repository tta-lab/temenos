// Package tools provides file reading, markdown parsing, URL fetching, and web search.
// These are the core implementations behind the temenos CLI subcommands.
//
// CommandHelp structs are the single source of truth for command help text —
// used by both cobra (--help) and the system prompt builder.
package tools
